snapshots:
  - name: orders_snapshot_check
    relation: ref('orders_snapshot_eph')
    config:
      schema: snapshots
      unique_key: id
      dbt_valid_to_current: "to_date('9999-12-31')"
      strategy: check
      table_format: iceberg
      external_volume: ICEBERGEXVOL
      check_cols: all